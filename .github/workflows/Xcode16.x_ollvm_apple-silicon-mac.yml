name: 编译 Hikari-LLVM15 工具链 (Apple Silicon 版)

on:
  # 手动触发编译
  workflow_dispatch:
    inputs:
      tag_name:
        description: '发布到 Release 的版本号 (如 v1.0-Sonoma)'
        required: true
        default: 'v1.0-M-Series'

# 赋予 Action 写入 Release 的权限
permissions:
  contents: write

jobs:
  build_toolchain:
    # 使用 macOS 15 运行器
    runs-on: macos-15

    steps:
      # --- 步骤 1：打印环境信息 (用户要求重点打印) ---
      - name: 1. 打印运行环境详细信息
        run: |
          echo "================ 系统信息 ================"
          echo "操作系统版本:"
          sw_vers
          echo ""
          echo "内核信息:"
          uname -a
          echo ""
          echo "处理器架构:"
          uname -m
          echo ""
          echo "CPU 具体型号:"
          sysctl -n machdep.cpu.brand_string
          echo ""
          echo "================ Xcode 信息 ================"
          echo "当前使用的 Xcode 路径:"
          xcode-select -p
          echo ""
          echo "Xcode 版本详细信息:"
          xcodebuild -version
          echo "=========================================="

      # --- 步骤 2：基础环境设置 ---
      - name: 2. 设置 Git 缓冲区
        run: git config --global http.postBuffer 524288000

      - name: 3. 安装编译依赖
        run: brew install ninja cmake

      # --- 步骤 4：递归克隆 Hikari-LLVM15 源码 ---
      - name: 4. 克隆源码 (Recursive)
        run: |
          git clone --recursive -b apple-arm64e-upstream-next https://github.com/LiuSky/Hikari-LLVM15.git

      # --- 步骤 5：执行 CMake 配置 ---
      - name: 5. 执行 CMake 配置
        run: |
          mkdir -p Build
          cd Build
          ABS_INSTALL_DIR=$(pwd)/install
          cmake -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_OSX_ARCHITECTURES=arm64 \
            -DLLVM_ENABLE_PROJECTS=clang \
            -DLLVM_TARGETS_TO_BUILD="X86;ARM;AArch64" \
            -DLLVM_ENABLE_ZSTD=OFF \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=13.0 \
            -DLLVM_CREATE_XCODE_TOOLCHAIN=ON \
            -DLLVM_INCLUDE_TESTS=OFF \
            -DLLVM_INCLUDE_EXAMPLES=OFF \
            -DLLVM_INCLUDE_UTILS=OFF \
            -DLLVM_INCLUDE_BENCHMARKS=OFF \
            -DLLVM_BUILD_TOOLS=OFF \
            -DLLVM_INSTALL_TOOLCHAIN_ONLY=ON \
            -DCMAKE_INSTALL_PREFIX="$ABS_INSTALL_DIR" \
            ../Hikari-LLVM15/llvm

      # --- 步骤 6：开始 Ninja 编译 ---
      - name: 6. 开始 Ninja 编译
        run: |
          cd Build
          ninja

      # --- 步骤 7：生成 Xcode 工具链结构 ---
      - name: 7. 安装并生成 Xcode 工具链
        run: |
          cd Build
          # 执行安装，将编译出的文件放入 install 目录
          ninja install
          # 生成 .xctoolchain 文件夹
          ninja install-xcode-toolchain

      # --- 步骤 8：打包工具链成品 ---
      - name: 8. 打包工具链成品
        run: |
          # 寻找生成的 .xctoolchain 文件夹
          TOOLCHAIN_PATH=$(find Build/install -name "*.xctoolchain" -type d | head -n 1)
          
          if [ -z "$TOOLCHAIN_PATH" ]; then
            echo "❌ 错误：未找到生成的工具链文件夹"
            find Build/install -maxdepth 4
            exit 1
          fi
          
          BASE_DIR=$(dirname "$TOOLCHAIN_PATH")
          FOLDER_NAME=$(basename "$TOOLCHAIN_PATH")
          
          echo "✅ 找到工具链: $FOLDER_NAME 在 $BASE_DIR"
          
          # 进入所在目录并打包
          cd "$BASE_DIR"
          tar -czvf ../../../Hikari-LLVM15-Toolchain.tar.gz "$FOLDER_NAME"
          echo "📦 打包完成"

      # --- 步骤 9：创建 Release 并上传成品 ---
      - name: 9. 上传到 Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag_name }}
          files: ./Hikari-LLVM15-Toolchain.tar.gz
          body: |
            ## Hikari-LLVM15 编译产物 (macOS 15 平台)
            
            **编译环境信息:**
            - **Runner OS:** macOS 15 (Sequoia)
            - **Architecture:** Apple Silicon (arm64)
            - **Xcode Version:** 自动使用当前系统默认版本
            - **Hikari Branch:** `apple-arm64e-upstream-next`
            
            **说明:** 
            此工具链直接支持集成到 Xcode 中使用。
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
